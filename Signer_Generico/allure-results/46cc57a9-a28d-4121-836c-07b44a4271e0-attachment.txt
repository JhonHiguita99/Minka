// Imprimir el comentario en la consola de Newman
console.log("Comentario del caso de prueba: ", pm.iterationData.get("comentario"));

pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

// Convertir la respuesta en JSON
let response = pm.response.json();

// Extraer tx_ref
let tx_ref = response.labels.tx_ref;

// Guardar en una variable de entorno en Postman
pm.globals.set("tx_ref", tx_ref);

// Mostrar en la consola de Postman
console.log("tx_ref:", tx_ref);